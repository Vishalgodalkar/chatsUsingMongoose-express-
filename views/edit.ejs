<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Your Chat</title>
</head>
<body>
    <h3>Edit your chat : <%= chat._id %></h3>
    <p>This chat is sent from <b><%= chat.from %> to <%= chat.to%></b></p>

    <form method="POST" action="/chats/<%=chat._id%>?_method=PUT">
        <textarea name="msg" rows="5" cols="16"><%= chat.msg %></textarea>
        <br>
        <button>Edit</button>
    </form>
</body>
</html>